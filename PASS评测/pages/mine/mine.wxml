<view wx:if = "{{!isLogin}}">
  <button type = "primary" bindtap = "jumpToLogin">登录</button>
</view>
<view wx:else class = "avatarSet">
  <image src = "{{userInfo.avatarUrl}}" class = "circle"/>
</view>