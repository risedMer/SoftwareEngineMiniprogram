/// <reference path="../../@types/index.d.ts" />
import { IProject } from '../../utils/project';
export declare function checkPageJSON(project: IProject, options: {
    pagePath: string;
    miniprogramRoot: string;
    useCache: boolean;
}): Promise<any>;
